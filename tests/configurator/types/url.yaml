_locked: true
file_name: url.yaml
root:
  bson_type:
    bsonType: string
    pattern: ^https?://[^\s]+$
  description: A valid URL
  json_type:
    format: uri
    type: string
  name: url
  required: false
  type: complex
