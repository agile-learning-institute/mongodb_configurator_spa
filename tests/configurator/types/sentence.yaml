_locked: true
file_name: sentence.yaml
root:
  description: A String of text, 0 to 255 characters with no special characters
  name: sentence
  required: false
  schema:
    pattern: ^[^\t\n]{0,255}$
    type: string
  type: simple
