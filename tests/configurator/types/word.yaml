_locked: true
file_name: word.yaml
root:
  description: A String of text, 1 to 40 characters with no spaces, or special characters
    like /t or /n
  name: word
  required: false
  schema:
    pattern: ^[^\s]{1,40}$
    type: string
  type: simple
